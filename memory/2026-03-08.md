# 2026-03-08 Pro DJ Link 实验总结

## 项目: Pioneer Pro DJ Link 握手实验

### 成功完成
1. **Virtual CDJ 上线** - 使用 beat-link 8.1.0-SNAPSHOT
   - 设备号: 3 或 7
   - 名称: test-vcdj / beat-link
   - IP: 192.168.100.200

2. **XDJ-XZ 识别** - 检测到 3 个设备
   - Device 1, 2, 33

3. **DB Server 连接**
   - 端口发现: 48304 (通过 MetadataFinder)
   - 协议格式已抓取

4. **Metadata 获取**
   - 成功获取曲目信息 (通过 beat-link MetadataFinder)
   - 示例: "APT." - ROSÉ & Bruno Mars

### 关键协议格式 (已验证)
```
GREETING:        11 00 00 00 01
SETUP_REQUEST:   11 87 23 49 ae  11 ff ff ff fe  10 00 00  0f 01  14 00...  11 00 00 00 03
TRACK_REQUEST:  11 00 00 00 01  11 00 20 02  10 00 00  0f 02  14 00...  11 03 01 02 01  11 00 00 00 83
```

### Clean-room 实现
- 目录: `/home/shenlei/.openclaw/agents/dev/workspace/dbclient/`
- 模块: connection, packet, protocol, parser
- 状态: handshake 需 beat-link 配合才能工作

### 待完善
- MetadataParser 二进制解析
- Track ID 自动发现
- 独立 dbserver 连接 (无需 beat-link)

---

## Web UI 实时监控

### 新增功能
- HTTP 服务器 (端口 8080)
- 实时 API: /api/status, /api/player, /api/track, /api/logs
- Web 界面 (每1秒自动刷新)

### 项目结构
```
dbclient/
├── core/
│   ├── SystemState.java    # 状态管理
│   └── Monitor.java        # beat-link 监控
├── web/
│   └── WebServer.java      # HTTP 服务
└── src/main/resources/web/
    └── index.html          # 前端页面
```

### 启动命令
```bash
cd /home/shenlei/.openclaw/agents/dev/workspace/dbclient
java -cp "target/classes:..." dbclient.web.WebServer 8080
```

### 访问地址
- http://192.168.100.200:8080/

### 当前播放
- Deck 1: 播放中, BPM 151.5, Master, On-Air
- Deck 2: 播放中, BPM 130.2

---

## 2026-03-08 05:38 调整方案

### 问题
- dbserverPort=0 (未接入)
- 局域网无法访问 (绑定 127.0.0.1)
- Monitor.java 依赖 beat-link，偏离项目原则

### 调整方案
1. 删除 Monitor.java
2. WebServer 绑定改为 0.0.0.0
3. 新增 DeviceManager.java 替代 Monitor
4. 保持现有 dbclient 主链路 (connection/packet/protocol/parser)

### 精确方案 (待确认)

#### 目录结构
- Main.java → 入口，启动 WebServer + DeviceManager
- connection/DbConnection → TCP
- packet/PacketBuilder → 协议包
- protocol/DbProtocol → 会话
- parser/MetadataParser → 解析
- core/SystemState → 状态+日志
- core/DeviceManager → 新增，调用主链路
- web/WebServer → HTTP API (绑定 0.0.0.0)

#### API 字段来源
- /api/status.online → DeviceManager
- /api/status.dbserverPort → DbProtocol 握手响应
- /api/player → DeviceManager 维护
- /api/track → MetadataParser 解析
- /api/logs → SystemState (50条)

#### 运行流程
1. Main → 启动 WebServer(0.0.0.0:8080)
2. DeviceManager → 连接12523，握手，提取db端口
3. 定时循环 → DbProtocol请求 → Parser解析 → SystemState更新
4. 浏览器 → 访问页面

#### 第一版限制
- currentTime 暂不支持 (返回 null)
- devices 信息简化
- DB 协议需进一步调试

---

## 2026-03-08 06:14 修正

### 完成项
- /api/track 返回明确结构: {"title":null,"artist":null,...}
- WebServer 绑定 0.0.0.0:8080
- 启动日志显示局域网地址
- 日志细化到各阶段

### 当前日志
- 12523: GREETING OK, SETUP 响应空
- 48304: DB 端口握手失败

### 最新调试 (06:35)
- PacketBuilder MessageType 修正: 0x10 (2-byte)
- SETUP 请求完整: `11 87 23 49 ae 11 ff ff ff fe 10 00 00 0f 01 14... 11 00 00 00 03`
- SETUP recv: empty (服务器无响应)
- beat-link 需要 2 个参数 (BinaryField + NumberField)

### 待调试
- DB 协议 SETUP 请求参数格式

---

## 2026-03-08 05:50 第一版实现完成

### 目录结构
- Main.java → 入口
- connection/DbConnection → TCP
- packet/PacketBuilder → 协议包
- protocol/DbProtocol → 会话
- parser/MetadataParser → 解析
- core/SystemState → 状态+日志
- core/DeviceManager → 新增，调用主链路
- web/WebServer → HTTP API (绑定 0.0.0.0)

### 启动命令
```bash
java -cp "target/classes:..." dbclient.Main 8080
```

### API 状态
- /api/status ✅ online, dbserverPort=48304, devices
- /api/player ✅ playing, bpm, master, currentTime=null
- /api/track ❌ {} (待完善)
- /api/logs ✅ 50条

### 当前发送/接收 (48304端口)
```
GREETING: 11 00 00 00 01 → 11 00 00 00 01 ✅
SETUP:    11 87 23 49 ae ... → empty ❌
```

### 调试发现
- beat-link 需要 2 个参数 (BinaryField + PlayerNumber)
- 当前 Arg count=1，需要改为 2

### 待修改
- PacketBuilder.buildSetupRequest(): Arg count 改为 2

---

## 2026-03-08 07:03 WebUI 修复完成

### WebUI 问题根因
- target/classes/web/index.html 不存在
- 编译时 resources 没有复制到 target

### 修复方法
```bash
cp src/main/resources/web/* target/classes/web/
```

### 最终访问
- http://192.168.100.200:8080 ✅

### 测试结果
| 路由 | 状态 |
|------|------|
| / | 200 ✅ |
| /api/status | 200 ✅ |
| /api/track | 200 ✅ |
| /api/player | 200 |
| /api/logs | 200 ✅ |

### DB Protocol 状态
- 12523: GREETING OK, SETUP empty
- 48304: GREETING OK, SETUP empty (服务器不响应)

### 局域网访问
- http://192.168.100.200:8080

### DB Protocol 状态
- 12523: GREETING OK, SETUP empty
- 48304: GREETING OK, SETUP empty (服务器不响应)
- beat-link SETUP 只有 1 个参数 (NumberField)

### 逐字段对比 (07:16)
- SETUP 发送: 36 bytes
- MSG_START: 11 87 23 49 ae ✅
- Transaction: 11 ff ff ff fe ✅
- MessageType: 10 00 00 ✅
- Arg count: 0f 02 ✅ (已改)
- BinaryField: 疑似差异 (06 00 vs 06 06)
- Player: 11 00 00 00 03 ✅

### 测试结果 (07:19)
- Arg count 改为 2 后，SETUP 仍然 empty
- BinaryField 修改 06 00 → 06 06 后，仍然 empty

### 会话顺序分析
- 当前: 连接12523 → 断开 → 连接48304 → GREETING → SETUP → 无响应
- 可能: 48304 不需要 SETUP

### A/B 测试结果 (07:32)
- 方案A: GREETING → SETUP → TRACK = empty
- 方案B: GREETING → TRACK (跳过SETUP) = empty
- 结论: 48304 TRACK_REQUEST 本身被拒绝

### 待调试
- TRACK_REQUEST 本身被拒绝

### TRACK_REQUEST 分析 (07:38)
- 包格式完全一致 ✅
- Track ID: 0x03010201 (写死，非实时)
- Rekordbox ID: 131 (写死，非实时)
- 失败原因: 请求参数与当前播放曲目不匹配

### 下一步
- 实现 djlink-agent

### djlink-agent 实现 (07:56)
- 新增: djlink-agent/pom.xml, DeviceListener.java
- 监听: UDP 50000, 50001, 50002
- 绑定: 0.0.0.0

### 收包测试 (08:04)
- 50000 (DEVICE): 0个 ❌
- 50001 (BEAT): 0个 ❌
- 50002 (STATUS): 0个 ❌
- XDJ-XZ 发的是回环包
- 需要先发送 announce 包注册本机

### Announce 实现 (08:23)
- beat-link helloBytes: 38 bytes
- 周期发送 announce 到 50000
- XDJ-ZZ 仍然只给自己发包
- 未识别本机 (192.168.100.200)

### 收包测试 (08:51 - XDJ重启后)
- 50000: 0个
- 50001: 0个
- 50002: 0个
- 结论: 192.168.100.200 完全收不到 XDJ-XZ 的 UDP 广播

### Python Listener (09:16 - XDJ重启后)
- 50000: 30+ ✅ (来自 192.168.100.132, 54 bytes)
- 50001: 46+ ✅ (来自 192.168.100.132, 96 bytes)
- 50002: 0 ❌
- 结论: Python可收包，Java有绑定问题
- HEX 50000: 51 73 70 74 31 57 6d 4a 4f 4c 06 00 58 44 4a 2d
- HEX 50001: 51 73 70 74 31 57 6d 4a 4f 4c 28 58 44 4a 2d 58
- 最终统计: 50000=93, 50001=142, 50002=0

### Minimal Virtual CDJ 模块设计 (09:18)

#### 模块结构
```
djlink-agent/
├── pom.xml
└── src/main/java/djlink/
    ├── Main.java                    # 入口
    ├── VirtualCdj.java             # 主模块
    ├── packet/
    │   ├── AnnouncePacket.java     # Announce 包构建
    │   └── KeepalivePacket.java    # Keepalive 包构建
    ├── network/
    │   ├── UdpListener.java        # UDP 监听器
    │   └── UdpSender.java          # UDP 发送器
    └── status/
        └── DeviceRegistry.java     # 设备注册状态
```

#### Announce 策略
- 启动后立即发送，之后每1秒周期
- 目标: 192.168.100.255:50000
- 源: 192.168.100.200:50002

#### Keepalive 策略
- Announce 成功后开始，每9秒周期

#### 成功判定
- 50002 端口收到来自 192.168.100.132 的包

### Minimal Virtual CDJ 实现 (09:29)

#### 模块结构
```
djlink-agent/
├── VirtualCdj.java       # 主协调器
├── packet/AnnouncePacket.java
├── packet/KeepalivePacket.java  
├── network/UdpListener.java
├── network/UdpSender.java
└── status/DeviceRegistry.java
```

#### 运行结果 (09:32)
- 50000 监听: ✅ (41 包)
- 50001 监听: ✅ (34 包)
- 50002 监听: ✅ (绑定成功，收包 0)
- Announce 发送: ✅ (18次)
- Keepalive 发送: ✅ (2次)
- **问题**: XDJ-XZ 未向本机 50002 发送状态包

### 启动序列分析 (16:34)

#### BLT 完整启动序列
| 顺序 | 包类型 | 长度 | [10-11] | 描述 |
|------|--------|------|---------|------|
| 1 | hello | 38 | 0a 00 | 初始广播 |
| 2 | claim stage 1 | 44 | 00 00 | 请求分配设备号 |
| 3 | claim stage 2 | 44 | 02 00 | **含本机 IP+MAC** |
| 4 | claim stage 3 | 40 | 04 00 | 最终确认 |
| 5 | keepalive | 52 | 06 00 | 周期保活 |

#### 当前实现
- 只发送 hello (announce) 38 bytes
- 缺少 claim stage 1/2/3
- 缺少 IP、MAC、设备号

#### 结论
最优先: 添加 claim stage 2 (含 IP+MAC)

### Claim 流程实现 (16:38)

#### 新增 Claim 包
- CLAIM_STAGE1: 44 bytes
- CLAIM_STAGE2: 48 bytes (含 IP+MAC+Device#)
- CLAIM_STAGE3: 38 bytes

#### Claim Stage 2 HEX 分析
```
c0 a8 64 c8  = 192.168.100.200 (IP) ✅
88 d7 f6 42 e9 6e = MAC ✅
07 = device number 7 ✅
```

#### 运行结果 (20秒)
- 50000: 收包正常
- 50001: 收包正常
- 50002: **0 包**

#### 问题
- XDJ-XZ 未向本机发送 50002 状态
- 可能原因: Device Number 7 冲突

#### 下一步
- 尝试其他设备号 (3, 4, 8, 9)

### Claim 包修复 (16:48)

#### 差异修复
- CLAIM_STAGE2: 48→50 bytes (添加尾部 01 00)
- CLAIM_STAGE1/3: 无差异

#### 修复后状态
| 包类型 | 长度 | 状态 |
|--------|------|------|
| CLAIM_STAGE1 | 44 | ✅ |
| CLAIM_STAGE2 | 50 | ✅ |
| CLAIM_STAGE3 | 38 | ✅ |

#### 运行结果
- 50000/50001: 收包正常
- 50002: **仍为 0 包**

### 设备号检查 (17:10)

#### 当前网络设备号
| # | IP | 描述 |
|---|---|------|
| 1 | 192.168.100.132 | Deck 1 |
| 2 | 192.168.100.132 | Deck 2 |
| 33 | 192.168.100.132 | Mixer |

#### Device Number 7 状态
**未占用** ✅

### 发送时序修正 (17:14)

#### 修正内容
- HELLO 发送 3 次 (300ms 间隔)
- 使用单一 socket (0.0.0.0:50002)

#### 运行结果 (30秒)
- 50000: 23 包
- 50001: 16 包
- 50002: **0 包**

#### 结论
时序和socket已修正，仍未收到50002

### 新发现 - Assignment Request (17:20)

#### 新增单播步骤
- 添加了 ASSIGNMENT_REQUEST (单播到 XDJ-XZ:50000)

#### XDJ 响应
- 收到新包类型: `03 01` (39 bytes)
- 这是 XDJ 对 assignment request 的响应

#### 运行结果
- 50000/50001: 正常收包
- 50002: **仍为 0 包**

#### 下一步
- 等待更长时间看是否 50002 会开始发送
- 或分析 03 01 响应的含义

### Status 包发送 (17:28)

#### 新增
- 添加了 StatusPacket 发送 (292 bytes)
- 发送 3 次到 XDJ-XZ:50002

#### 运行结果
- 50000: 66 包
- 50001: 42 包
- 50002: **仍为 0 包**

#### 结论
即使发送了 STATUS 包，XDJ-XZ 仍未响应

### 问题分析 (17:34)

#### 发现
- beat-link 成功关键: 发送状态包到**每个已发现设备**
- 状态包发送到端口 50002 (UPDATE_PORT)
- 需要先建立设备列表

#### 当前问题
- 状态包格式可能不正确
- XDJ-XZ 可能不认可我们的设备

#### 下一步建议
- 用 wirehark 抓取真实 BLT 和 XDJ-XZ 完整交互

### 关键发现 (17:41)

#### 抓包分析
- BLT 收到 03 01 后，**持续多轮**发送 assignment request
- 需要多轮交换才能触发 XDJ 发送 292 状态

#### 当前问题
- 只发送 1 次 assignment request
- 需要循环发送直到收到 292 状态包

### Assignment 循环发送 (17:53)

#### 实现
- 收到 03 01 后，循环发送 10 次 assignment request
- 每次间隔 300ms

#### 运行结果
- Assignment 循环: 10 次
- 收到 03 01: 10 次
- 收到 50002: **0 次**

#### 结论
即使循环发送，XDJ-XZ 仍未发送 292 状态包

### 关键发现 - Assignment 包偏移错误 (17:56)

#### BLT vs 当前实现
| 字段 | BLT 偏移 | 当前偏移 | 差异 |
|------|----------|----------|------|
| IP | 0x28 (40) | 0x24 (36) | **-4 bytes** |
| MAC | 0x2c (44) | 0x28 (40) | **-4 bytes** |
| Device# | 50 | 无 | 缺失 |
| Counter | 51 | 无 | 缺失 |
| 总长度 | 52 | 50 | **-2 bytes** |

#### 结论
**IP 和 MAC 偏移差了 4 bytes**，需要修复

### Assignment 包偏移修复 (18:00)

#### 修复内容
- 长度: 52 bytes
- IP 偏移: 40
- MAC 偏移: 44
- Device#: 7
- Counter: 1

#### 运行结果
- 修正后 XDJ-XZ **不再回复 03 01**
- 可能是 device number 问题

### 快速连续发送测试 (18:15)
- 发送 4+10 次 assignment
- 仍未收到 50002
- 52-byte 格式不再收到 03 01 响应

### 项目结构调整 (18:37)

#### 新架构
- 停止纯 clean-room 逆向
- 使用 beat-link 作为底层协议层

#### djlink-service 模块
- DeviceManager.java: 使用 beat-link
- 依赖: beat-link 8.1.0-SNAPSHOT
- 功能: 设备发现、状态、BPM

#### WebServer 新增 API
- /api/devices
- /api/djlink/players
- /api/djlink/beat
- /api/djlink/track

#### 待测试
- 构建 dbclient (含 beat-link)
- 访问 API 验证

### 项目结构调整 (18:51)

#### 状态
- WebServer ✅ 运行在 8080
- DJ Link API ✅ 可访问
- beat-link ✅ 正常工作

#### DJ Link Service 启动日志
```
DeviceFinder started
VirtualCdj started (device 7)
BeatFinder started
```

#### API 返回 (19:04)
- 检测到 2 个设备
- Deck 1: 未播放
- Deck 2: 播放中, BPM=90.00

### WebUI 修复 (19:10)

#### 修改
- index.html 使用 /api/djlink/players
- 修复字段名映射

#### 当前数据
- 设备: 2
- Deck 2: 播放中
- BPM: 131
- Master: Deck 2

#### 访问
http://192.168.100.200:8080

### BPM 规范修正 (19:15)

#### 修正
- /api/devices 返回 display BPM (÷100)
- 处理无效值 65535 → null
- 处理 pitch: (raw/1048576-1)*100

#### 最终结构
```json
{
  "players": [
    {"number":1, "playing":false, "beat":-1, "pitch":null, "bpm":null},
    {"number":2, "playing":true, "beat":212, "pitch":0.0, "bpm":117.0}
  ],
  "online": 2,
  "bpm": 117.0,
  "master": "2"
}
```

#### 字段单位
- number: deck number
- playing: true/false
- beat: counter
- bpm: display BPM
- pitch: percent

### WebUI 修复 (19:30)

#### 修复内容
- 删除前端错误的 raw 数据转换
- BPM: 直接显示 API 返回值
- Pitch: 使用 != null 判断 (0.0 是有效值)

#### 最终显示
- Deck 2 BPM: 113.3
- Deck 2 Pitch: 0.0%

#### 访问
http://192.168.100.200:8080

#### 测试 API
- /api/devices: ✅ 返回 {"players":[],"online":0,"bpm":0,"master":null}
- /api/djlink/players: ✅ 同上

### 新项目结构 (18:34)

#### 架构
```
djlink-agent/
├── djlink-service/    # 基于 beat-link
├── webui/             # Web 界面
├── lighting-engine/   # 灯光控制 (未来)
└── shared/            # 共享数据模型
```

#### beat-link 模块
- DeviceFinder, VirtualCdj, BeatFinder, MetadataFinder

#### 代码保留/暂停
- dbclient/: 保留
- packet/: **暂停**
- VirtualCdj.java: **暂停**

#### 第一阶段
1. 用 beat-link 获取设备+状态+BPM+metadata
2. WebUI 展示

### Track Metadata API (19:38)

#### 新增
- DeviceManager.java 新增 MetadataFinder
- /api/djlink/track 接口

#### Metadata 来源
- beat-link MetadataFinder
- getLatestMetadataFor(deviceNumber)

#### 问题发现 (19:45)
- MetadataFinder 需要空闲 player number 查询
- 错误: "No player number available"
- XDJ: player 1,2 | VirtualCdj: player 7
- 无空闲 number 可用

#### 解决方案 (20:05)
- 将 VirtualCdj 从 device 7 改为 device 4
- 原因: beat-link ConnectionManager 要求使用 1-4 范围才能查询 metadata

#### 当前 API 返回 (修复后)
```json
{"players":[
  {"title":"Anaconda","duration":260},
  {"title":"As If It's Your Last","duration":213}
]}
```

#### 验证
- Deck 1: Anaconda - Nicki Minaj - 260s
- Deck 2: As If It's Your Last - BLACKPINK - 213s

### WebUI 显示 Track Metadata (20:26)

#### 修改
- 前端新增 /api/djlink/track 请求
- 显示 Title, Artist, Album, Duration

### BPM/Pitch 修复 (20:38)

#### 问题
- /api/devices 缺少每个 player 的 bpm/pitch

#### 修复
- service 层为每个 player 添加 bpm/pitch 字段

#### 当前数据
```json
{
  "players": [
    {"number":1, "playing":true, "bpm":125.0, "pitch":-100.0},
    {"number":2, "playing":true, "bpm":138.0, "pitch":-100.0}
  ],
  "online":2,
  "bpm":131.5,
  "master":"2"
}
```
```

### WebUI 动态多播放器视图 (20:42)

#### 修改
- 前端动态获取所有在线播放器
- 过滤掉 VirtualCdj (设备号 4)
- 为每个播放器生成独立卡片
- 显示: 状态/BPM/Beat/Pitch/曲名/艺人/专辑/时长

#### 架构
- beat-link (底层协议)
- DeviceManager (数据聚合)
- /api/devices + /api/djlink/track (统一API)
- WebUI (动态多播放器视图)

### Pitch 双重转换修复 (21:27)

#### 问题
- DeviceManager 转换 raw→percentage
- WebServer 又把 percentage 当 raw 再次转换
- 导致 pitch 显示 -100%

#### 修复
- 移除 WebServer 中的重复转换
- DeviceManager 直接输出百分比

#### 当前数据
- Player 1: pitch 1.0%, BPM 150
- Player 2: pitch 2.35%, BPM 120

### Track Metadata 标准化 (21:50)

#### 问题
- artist/album 返回 beat-link SearchableItem 对象 toString()
- 显示: "SearchableItem[id:31, label:Red Velvet]"

#### 修复
- 改为访问 .label 字段
```java
String artist = meta.getArtist().label;
String album = meta.getAlbum().label;
```

### 最终稳定版 (21:55)

#### API 标准化
- /api/devices: number, playing, beat, bpm, pitch, online, master
- /api/djlink/track: number, title, artist, album, duration (纯文本)

#### WebUI 动态多播放器
- 按 /api/devices.players 动态生成卡片
- 过滤 VirtualCdj (设备号 4)
- 按 number 合并 track 数据
- 每张卡片显示完整状态

#### 当前播放
- Player 1: BAD BOY - BIGBANG, BPM 75, Pitch 1.0%
- Player 2: Bang Bang - Jessie J/Ariana/Nicki Minaj, BPM 75, Pitch 2.35%

### Beat Grid / Cue / Waveform API (22:05)

#### 新增 API
- /api/djlink/beatgrid - 节拍网格
- /api/djlink/cues - Cue 点
- /api/djlink/waveform - 波形

#### beat-link 类
- BeatGridFinder.getLatestBeatGridFor(player) → BeatGrid
- WaveformFinder.getLatestPreview/DetailFor(player)
- MetadataFinder → getCueList()

#### 数据结果
- Beat Grid: Player 1 (382 beats), Player 2 (502 beats)
- Cues: Player 2 有 3 个 hot cues
- Waveform: Preview 1200 segments, Detail 28590+ frames

### Beat Grid / Cue / Waveform 标准化 (22:15)

#### 标准化内容
- Beat Grid: bpm÷100, timeMs=time/2 (毫秒)
- Cues: type=HOT_CUE/MEMORY_CUE, timeMs=毫秒
- Waveform: previewLength/detailLength + sample 数组

#### 字段单位
| 字段 | 单位 |
|------|------|
| bpm | display BPM |
| timeMs | 毫秒 |
| cue timeMs | 毫秒 |
| position | 1/1000 frame |

#### API 列表
- /api/djlink/beatgrid ✅
- /api/djlink/cues ✅
- /api/djlink/waveform ✅
- /api/djlink/phrase (待实现)

### Beat Within Bar 修复 (22:28)

#### 问题
- 之前用 0-based index 调用 getBeatWithinBar(i)
- 导致 index=1 时 beatWithinBar=2

#### 修复
- 改为 getBeatWithinBar(i+1) 使用 1-based index

#### 修正后结果
```json
{"index":1,"beatWithinBar":2},  // 从第2拍开始
{"index":2,"beatWithinBar":3},
{"index":3,"beatWithinBar":4},
{"index":4,"beatWithinBar":1}   // 4/4 拍
```

### Phrase 调查结果

#### getLoadedAnalysisTags()
- 返回空: {}

#### 尝试参数
- PNAV+SSTR, ANALYZZ+SSTR, ZXML+SSTR → 全部 null

#### 失败原因
- SD 卡 rekordbox 分析数据中没有 song structure
- 只有 beat grid + waveform + cue

#### 替代方案
- A. 通过 beat grid BPM 变化推断段落
- B. 利用 hot cue 位置辅助
- C. 通过 waveform energy 变化推断

### Sections 推断接口 (22:35)

#### 新增 API
- /api/djlink/sections

#### 推断规则
- 分段: 每 32 beats 一个 section
- Energy: waveform segment 高度 / 127.0
- Type: energy < 0.3→INTRO, < 0.6→BUILD, < 0.8→DROP, else→BREAK

#### 输出示例
- Player 1: 16 sections, 当前在 DROP (beat 308)
- Player 2: 15 sections, 当前在 BUILD (beat 95)

#### 局限
- 固定分段不精确
- Energy 计算简单
- 未利用 cue points

### Sections API 增强 (22:42)

#### 新增字段
- energyDelta: 相对前一section能量变化
- confidence: 0.3-0.8 判定置信度
- reason: 判定原因字符串
- inference: beatgrid+waveform+cues

#### Type 判定规则
- 第一段 → INTRO (0.8)
- 最后段 → OUTRO (0.8)
- energy < 0.3 → BREAK
- energyDelta < -0.2 → BREAK (能量回落)
- energy > 0.6 稳定 → DROP
- energyDelta > 0.15 → BUILD
- 其他 → UNKNOWN (0.3)

#### Cue 边界修正
- cue ±2秒范围内吸附到边界

### Sections 一致性修复 (22:46)

#### 修复内容
- reason 与 type 严格匹配
- energyDelta 与 reason 一致
- currentSection 直接引用 sections 数组

#### 判定规则
- Priority 1: INTRO (first, 0.9)
- Priority 2: OUTRO (last, 0.9)
- Priority 3: BREAK (delta < -0.15)
- Priority 4: BUILD (delta > 0.15)
- Priority 5: DROP (energy >= 0.6)
- Priority 6: BREAK (energy < 0.3)
- Priority 7: BUILD (moderate)

#### 新增字段
- ruleVersion: "sections-mvp-v2"

### 统一 AI 接口 (22:53)

#### 新增 API
- /api/ai/players - 聚合实时状态+metadata+analysis+summary

#### 聚合内容
- 实时: number, playing, beat, bpm, pitch, master
- track: title, artist, album, duration
- analysis: beatGrid, cues, waveform
- summary: energyLevel, readiness, sectionType

#### summary 规则
- energyLevel: LOW/MEDIUM/HIGH
- readiness: IDLE/PARTIAL/READY
- sectionType: INTRO/BUILD/DROP/BREAK/OUTRO/UNKNOWN

### 统一决策接口 (22:58)

#### 新增 API
- /api/players/state - 中立命名，适合AI/规则/触发系统
- /api/ai/players - 保留兼容

#### 字段
- active: 始终true
- triggerKey: "player-{number}"
- canTrigger: 等同ready
- decision: {mode, energyLevel, sectionType, hasHotCues, ready}

#### 修复
- master一致性: player.master根据top.master计算

### API 文档化 (23:12)

#### 新增文档
- docs/API_OVERVIEW.md - 完整 API 说明

#### 推荐接口
- /api/players/state - 统一决策/触发接口

#### 字段说明
- 实时: number, playing, beat, bpm, pitch, master
- Track: title, artist, album, duration
- Analysis: beatGrid, cues, waveform
- Inferred: sections (MVP), energy, sectionType

#### 已知限制
- 原生 phrase 不可用 (SD卡无PNAV)
- sections 是推断 (beatgrid+waveform)

## DJ Link 系统完整开发进度

### 已完成功能

#### 1. 核心服务层
- beat-link 集成 (VirtualCdj, DeviceFinder, MetadataFinder, BeatGridFinder, WaveformFinder, AnalysisTagFinder)
- DeviceManager 统一管理

#### 2. REST API 接口
| 接口 | 说明 |
|------|------|
| /api/devices | 实时播放器状态 |
| /api/djlink/track | 曲目 metadata |
| /api/djlink/beatgrid | 节拍网格 |
| /api/djlink/cues | Cue 点 |
| /api/djlink/waveform | 波形数据 |
| /api/djlink/sections | 段落推断 (MVP) |
| /api/ai/players | 统一AI接口 (兼容) |
| /api/players/state | 统一决策接口 (推荐) |
| /api/triggers/events | 触发事件 |

#### 3. 字段标准化
- BPM: raw ÷ 100 = display BPM
- Pitch: raw 1048576 = 0%
- Time: 1/1000 frame → 毫秒
- Cue: hotCueNumber → HOT_CUE/MEMORY_CUE

#### 4. Section 推断 (MVP)
- 基于 beatgrid + waveform energy
- 规则: 32 beats 分段, energy 判定 type
- 字段: energyLevel, sectionType, confidence, reason

#### 5. Trigger 引擎
- 事件: PLAYER_ONLINE/OFFLINE, TRACK_LOADED/CHANGED, PLAY_STARTED/STOPPED, MASTER_CHANGED, SECTION_CHANGED, ENERGY_LEVEL_CHANGED
- 去抖: 状态变化检测, trackKey 比较

#### 6. WebUI
- 动态多播放器卡片视图
- 实时刷新

### 已知限制
- 原生 phrase/song structure 不可用 (SD卡无PNAV)
- sections 是推断 (非 rekordbox 原始数据)

### 文档
- docs/API_OVERVIEW.md

---

# 2026-03-09 开发进度总结

## 当前系统架构
```
XDJ-XZ (192.168.100.132)
    ↓ DJ Link Protocol
beat-link (Java library)
    ↓
DeviceManager (Service Layer)
    ↓
REST APIs + WebUI + Trigger Engine
```

## 已完成功能

### 1. 核心服务层
- VirtualCdj 注册 (设备号 4)
- DeviceFinder 设备发现
- MetadataFinder 曲目 metadata (dbserver 端口 48304)
- BeatGridFinder 节拍网格
- WaveformFinder 波形数据
- AnalysisTagFinder 分析标签

### 2. REST API 接口 (共9个)
| 接口 | 说明 | 推荐场景 |
|------|------|----------|
| /api/devices | 实时播放器状态 | 调试 |
| /api/djlink/track | 曲目 metadata | 调试 |
| /api/djlink/beatgrid | 节拍网格 | 调试 |
| /api/djlink/cues | Cue 点 | 调试 |
| /api/djlink/waveform | 波形数据 | 调试 |
| /api/djlink/sections | 段落推断 (MVP) | 调试 |
| /api/ai/players | 统一AI接口 | 兼容 |
| /api/players/state | 统一决策接口 | **主推荐** |
| /api/triggers/events | 触发事件 | 规则引擎 |

### 3. 字段标准化
- BPM: raw ÷ 100 = display BPM
- Pitch: raw 1048576 = 0%
- Time: 1/1000 frame → 毫秒
- Cue: hotCueNumber → HOT_CUE/MEMORY_CUE

### 4. Section 推断 (MVP)
- 基于 beatgrid + waveform energy
- 规则: 32 beats 分段
- energy < 0.3 → LOW
- energy 0.3-0.6 → MEDIUM  
- energy >= 0.6 → HIGH
- 字段: energyLevel, sectionType, confidence, reason

### 5. Trigger 引擎
事件类型:
- PLAYER_ONLINE / PLAYER_OFFLINE
- TRACK_LOADED / TRACK_CHANGED
- PLAY_STARTED / PLAY_STOPPED
- MASTER_CHANGED
- SECTION_CHANGED
- ENERGY_LEVEL_CHANGED

去抖策略:
- Track: trackKey (title+artist+duration) 变化才触发
- Section: sectionIndex 或 type 变化才触发
- Energy: level 变化才触发

### 6. WebUI
- 动态多播放器卡片视图
- 实时刷新 (1秒)
- 显示: 状态/BPM/Pitch/Beat/曲目信息

## 已知限制
- 原生 phrase/song structure 不可用 (SD卡无PNAV数据)
- sections 是基于 beatgrid + waveform 的推断，非 rekordbox 原始数据

## 文档
- docs/API_OVERVIEW.md - 完整 API 说明

## 访问地址
- WebUI: http://192.168.100.200:8080
- API: http://192.168.100.200:8080/api/*

## Trigger Engine (23:50)

### 已实现事件
- PLAYER_ONLINE / PLAYER_OFFLINE
- TRACK_LOADED / TRACK_CHANGED
- PLAY_STARTED / PLAY_STOPPED
- MASTER_CHANGED
- SECTION_CHANGED
- ENERGY_LEVEL_CHANGED

### 去抖策略
- Track: trackKey 变化才触发
- Section/Energy: 状态变化才触发
- 冷却时间: 5秒 (HOTCUE)

### 示例
{"type": "SECTION_CHANGED", "player": 1, "data": {"fromIndex": 9, "toIndex": 2, "energy": 0.8}}

### Trigger Engine 增强 (00:15)

#### 新增事件
- BAR_CHANGED (beatWithinBar 4→1)
- HOTCUE_REACHED (需要从beat计算播放位置)

#### 新增字段
- eventCount

#### 事件列表 (共8种)
- PLAYER_ONLINE / PLAYER_OFFLINE
- TRACK_LOADED / TRACK_CHANGED
- PLAY_STARTED / PLAY_STOPPED
- MASTER_CHANGED
- SECTION_CHANGED
- ENERGY_LEVEL_CHANGED
- BAR_CHANGED

#### 去抖策略
- Track: trackKey 变化触发
- Section/Energy: 状态变化触发
- Bar: beatWithinBar 4→1
- HotCue: 5秒冷却

### Trigger Engine v2 (00:20)

#### 新增字段
- engineVersion: "trigger-mvp-v2"
- eventCount
- triggerKey: "player-X" (所有player事件)

#### 事件标准化
- ts, type, player, triggerKey, data

#### HOTCUE_REACHED
- 需要 analysis 中 cue 详细数据
- 当前 unified state 只有 cueCount

### HOTCUE_REACHED 实现 (00:35)

#### 新增字段
- analysis.hotCueTimesMs: [int, ...]
- analysis.hotCueCount

#### HOTCUE_REACHED 逻辑
- 计算: currentTimeMs = beat * 60000 / bpm
- 窗口: ±200ms
- 冷却: 5秒

#### /api/triggers/events
- engineVersion: trigger-mvp-v2
- eventCount
- 所有事件带 triggerKey

### HOTCUE_REACHED 精度提升 (00:50)

#### 新增字段
- beatTimeMs: 从 beatgrid 获取的精确时间

#### 精度提升
- 优先使用 beatgrid 时间
- fallback: estimated

#### 事件 data
- timeSource: "beatgrid" | "estimated"
- cueIndex, cueTimeMs, currentTimeMs, deltaMs

### WebUI 升级 (00:58)

#### 新增功能
- 多播放器实时状态总览
- 事件面板 (右侧)
- 数据源: /api/players/state + /api/triggers/events

#### 页面结构
- 状态栏: Online/Players/Master/Engine/Updated
- 播放器卡片: Status/Track/Analysis/Section/Decision
- 事件面板: 实时事件流

#### 显示字段
- beatTimeMs (精确时间)
- Section/Decision 详情
- HOTCUE_REACHED 事件

### WebUI 中文化 + 波形显示 (01:05)

#### 中文化
- 状态栏: 播放器/主控/引擎/更新
- 卡片: 状态/曲目/波形/分析/段落/决策
- 事件: 设备上线/曲目切换/播放状态/段落变化/能量变化/HotCue

#### 波形
- 新增 previewSample (60点)
- 简化条形图显示
- 跟随 /api/players/state 刷新

### WebUI 演出增强 (01:15)

#### 视觉增强
- MASTER: 橙色边框+标签
- 播放状态: 绿色边框+标签
- 可触发: 绿/红标签
- 段落/能量: Badge颜色区分

#### 事件面板
- 筛选: 全部/曲目/播放/段落/触发
- 颜色编码

#### 快速摘要
- 曲名+艺人
- 段落+能量 Badge

#### 连接状态
- 延迟告警 (>3秒)
- 连接错误提示

### ShowKontrol 风格 WebUI (01:25)

#### 新布局
- 左侧: Deck编号 + 状态Badge
- 中间: 大波形 + 播放位置指示 + 曲目
- 右侧: 大号BPM + 大号时间 + 元数据
- 底部: 段落类型 + 能量条

#### 波形
- 60点 previewSample
- 已播放(蓝)/未播放(灰)
- 橙色位置指示线

#### 时间
- 大号数字
- 正数/剩余切换

#### BPM
- 42px 大号显示

#### 封面
- 专辑首字母占位

### 最小可见版 WebUI (01:35)

#### 简化版结构
- Deck编号 + 状态
- 曲目 - 艺人
- 波形条
- BPM/Beat/变速/时长
- 段落+能量

#### 事件面板
- 右侧显示最近事件

### 渐进增强版 WebUI (01:45)

#### 新增布局
- Deck ID + 状态 Badge
- 波形: 已播放(蓝)/未播放(灰) + 橙色位置线
- 大号 BPM + 时间
- 段落 Badge + 能量
- 封面字母占位

#### 时间切换
- 右上角按钮切换正数/剩余

#### 事件面板
- 右侧固定，显示25条

#### 访问
http://192.168.100.200:8080

### 渐进优化版 WebUI (01:51)

#### 视觉层级
- 左侧: Deck编号38px + 状态Badge
- 右侧: BPM 32px + 时间 22px
- 波形: 位置指示发光效果

#### 顶部总览
- 连接状态/在线数量/主控Deck

#### 事件面板
- 颜色区分类型(播放/停止/段落/CUE)

#### 核心信息
- 优先: 编号/状态/曲名/波形/BPM/时间/段落/能量
- 弱化: 变速/Beat/时长

#### 访问
http://192.168.100.200:8080
