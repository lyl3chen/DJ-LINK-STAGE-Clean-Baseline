
## 00:02 Phase-2.1 LTC 可视化验证页（新工作区）完成
- 用户在迁移后要求继续 Phase-2.1，并提出4项验收：
  1) LTC 动态绿条（signalLevel）
  2) 大字号 Live Timecode
  3) 诊断卡（Status/Target/FPS，异常红色错误）
  4) 改 deviceName/fps 后 Save 立即生效（热重载）
- 已在新工作区实现并运行：
  - `dbclient/src/main/resources/web/index.html`
    - 新增 LTC 电平条 + 百分比
    - 新增 Live Timecode 大字号显示
    - 新增诊断卡（正常/错误态）
  - `dbclient/src/main/java/dbclient/sync/SyncOutputManager.java`
    - `applySettings()` 改为 stop->start，保存配置后强制热重载驱动
  - `README_USER.md`
    - 同步更新外行验收步骤与观察点
- 服务验证：
  - 在新工作区编译、重建、重启后 `/api/sync/state` 正常返回 LTC 实时字段。
- 代码提交（新工作区）：`4e4063b`
  - `feat(phase2.1): add LTC live monitor, diagnostic card and hot-reload save behavior`

## 00:02-00:09 LTC时间码不动问题定位
- 用户反馈：CDJ 正在播放，但 Live Timecode 不动。
- 现场诊断结果：
  - LTC 驱动已运行（`running=true`）
  - `signalLevel` 有值（绿条跳动）
  - 但 `seconds` 持续为 0.0，导致 `timecode` 固定 `00:00:00:00`
- 已进行修复尝试：
  1) `SyncOutputManager` 主播放源选择改为：优先 master+playing，其次 playing，再 fallback 第一个
  2) 取时逻辑改为 `currentTimeMs` 不存在则回退 `beatTimeMs`
- 当前状态：问题仍在，确认属于“主时间注入链路”剩余 bug；已向用户说明并继续修复。

## 00:09 播放源手动选择功能已实现
- 用户要求：LTC 播放源改为手动可选（player1/2，3/4在线才显示），并保留“跟随 master player”。
- 已完成：
  - 后端新增 `sync.sourceMode`（`master`/`manual`）与 `sync.masterPlayer` 逻辑接入
  - I/O Setup 新增：
    - 播放源模式下拉（跟随Master/手动）
    - 手动 Player 下拉（按在线列表动态显示1~4）
  - 保存配置后立即生效（热重载）
- 配置文件更新：
  - `user_settings.json` 增加 `sync.sourceMode`
- 提交记录（新工作区）：`ad326a9`
  - `feat(sync): add manual source player selection (1-4 online) with master-follow mode`

## 00:29 LTC 核心行为逻辑增强（静默/异常/手动源）
- 用户提出核心规则：
  - 停止/暂停/CUE 时 LTC 必须静音（不可卡帧）
  - scratch/search 需要实时追踪
  - 声卡断开进入 ERROR 不崩溃
  - 源掉线快速切断；Auto Master 自动切主
  - 采样率不支持时自动 fallback 到 48000/44100
- 已完成实现（新工作区）：
  - `LtcDriver` 增加状态机字段：`outputState`、`sourcePlaying`、`sourceActive`
  - 输出循环增加 500ms 源超时判断，非播放态输出静音
  - 写入异常/设备丢失转 `ERROR`，并保持进程稳定
  - 音频格式选择增加 fallback：请求值 -> 48000 -> 44100
  - 状态返回新增：`outputState` 等诊断字段
- 同时按用户需求完善播放源选择：
  - 新增 `sourceMode`（master/manual）+ `masterPlayer`
  - UI 支持手动 Player 选择（在线列表动态显示 1~4）
- 当前运行状态确认：`/api/sync/state` 可见 `outputState=SILENT_SOURCE_TIMEOUT` 等新字段。
- 相关提交（新工作区）：
  - `f83cd39` `feat(ltc): add manual source selection and robust silence/error/fallback behaviors`

## 01:06 LTC电平与声卡选择交互优化完成
- 用户反馈两项问题：
  1) CDJ播放且时间码跳动时，电平条仍为0%
  2) 声卡名手动输入不专业，需改为下拉列表
- 已完成实现（新工作区）：
  1) 电平计算改进：
     - `LtcDriver` 音频输出循环由峰值改为 RMS 振幅计算
     - `signalLevel` 增加平滑处理，提升可视电平稳定性
  2) 声卡下拉：
     - `JettyServer` 通过 `AudioSystem.getMixerInfo()` 枚举当前声卡
     - `/api/config` 增加 `_runtime.audioDevices` 返回
     - I/O Setup 将 `LTC 声卡` 改为 Dropdown，只能选系统存在设备（含 default）
- 同步增强：
  - 保持静默/超时/设备错误/fallback 规则有效
  - source 逻辑与 UI 继续联动
- 运行验证：`/api/config` 已返回 `_runtime.audioDevices`，`/api/sync/state` 返回 LTC 实时字段。
- 提交记录（新工作区）：`de50f26`
  - `feat(ltc): rms meter + audio device dropdown from runtime mixers + stricter source silence rules`

## 02:09 声卡下拉可读性问题
- 用户反馈：I/O Setup 下拉项“看起来不是声卡”。
- 已解释：当前显示的是 ALSA 底层设备标识（如 default/hw/plughw），确实不够友好。
- 已确认下一步改进方向：
  - 将下拉展示改为“人话名称 + 底层ID”
  - 示例：系统默认输出 / 主板声卡 / USB声卡通道（括号保留底层ID）
  - 保留选择准确性，同时提升非技术用户可读性。

## 02:11 声卡下拉进一步人话化
- 用户要求：下拉中只显示“实际可用于输出的声卡型号”。
- 已完成：
  - 后端仅保留可输出设备（SourceDataLine 可用）
  - 下拉显示使用人话标签：
    - `系统默认输出`
    - 声卡型号短名（去除底层 `[hw:x]` 噪声）
  - 前端下拉显示 label，value 仍保留底层 name 便于精确绑定
- 部署验证：`/api/config` 返回 `_runtime.audioDevices` 已包含人话化 label。
- 提交记录（新工作区）：`38b0c9b`
  - `ux(audio): show only output-capable devices and human-friendly sound card labels`

## 02:19 声卡下拉专业化与实时刷新完成
- 用户要求进一步优化声卡下拉：
  1) 读取更详细设备信息（name+description），过滤仅录音设备
  2) 显示格式改为：`[序号] 友好名称 (驱动代号)`
  3) 每次打开 I/O Setup 或点击下拉时实时刷新设备列表
  4) 同步确认电平与停止归零逻辑
- 已完成实现（新工作区）：
  - `JettyServer`：
    - `listAudioDevices()` 增加输出能力过滤（SourceDataLine）
    - 返回字段补充 `index/label/desc/vendor`
  - `index.html`：
    - 下拉项显示格式改为“序号 + 友好名 + 驱动代号”
    - I/O Tab 打开时自动 `loadSettings()` 刷新设备
    - 下拉点击时再次拉取最新设备列表
  - LTC 逻辑保留 RMS 电平、静音归零、错误处理
- 运行状态观察：
  - 当前后端状态曾出现 `SILENT_SOURCE_OFFLINE` 且 players 为空，提示当前无有效播放源（需检查扫描/在线状态）
- 提交记录（新工作区）：`aed8e67`
  - `ux/audio): device list refresh on IO open + dropdown labels with index and driver code; ltc rms/silence refinements`

## 02:28 时间码驻留逻辑调整完成
- 用户要求：停止/暂停时 LTC 继续静音，但 Live Timecode 不归零，保留最后一帧；并在无输出时将时间码变灰。
- 已完成：
  - `LtcDriver` 去除停止时清零逻辑，改为“静音+驻留最后帧”
  - 前端 `ltc-timecode` 新增 `dim` 样式，依据 `outputState != OUTPUTTING` 自动灰显
- 部署：已重新编译并重启服务。
- 提交记录（新工作区）：`e94d494`
  - `ux(ltc): keep last timecode on stop and dim live monitor when silent`

## 02:36 LTC电平语义说明
- 用户询问：LTC 电平当前和什么对象/数值关联。
- 已明确说明：
  - 电平条绑定 `LtcDriver` 实际输出音频的 RMS 振幅（`signalLevel`）
  - 前端读取来源：`/api/sync/state -> drivers.ltc.signalLevel`
  - 影响因素：`gainDb`、是否 `OUTPUTTING`、生成波形振幅
  - 非直接绑定 BPM 或播放进度。

## 02:45 进入 Phase-2.2（MTC）并完成第一版实装
- 用户正式要求进入 Phase-2.2：MTC（MIDI Timecode）实装。
- 已完成第一版落地（新工作区）：
  1) `MtcDriver` 真实发送逻辑：
     - 使用 `javax.sound.midi`
     - 发送 MIDI Quarter Frame (0xF1)
     - 继承与 LTC 同步源门控（sourcePlaying/sourceActive）
     - 暂停/断流进入 SILENT_*，端口异常进入 ERROR
  2) MIDI 设备自动识别：
     - `JettyServer` 在 `/api/config` 的 `_runtime` 增加 `midiOutDevices`
  3) UI（I/O Setup）同步：
     - 新增 MTC 端口下拉
     - 新增 MTC 脉冲指示灯（发送时闪烁）
     - 新增 MTC 诊断卡（Outputting/Idle/Error）
- 验证：
  - `/api/config` 可见 `_runtime.midiOutDevices`
  - `/api/sync/state` 可见 MTC 驱动状态字段
- 提交记录（新工作区）：`8073e98`
  - `feat(phase2.2): implement MTC quarter-frame output with MIDI out dropdown and live pulse diagnostics`

## 02:51 MtcDriver 编辑失败提示澄清
- 用户反馈 OpenClaw 显示“Edit MtcDriver.java failed”。
- 已核查：
  - `MtcDriver.java` 文件存在且内容完整
  - `dbclient mvn -q compile -DskipTests` 编译通过
- 结论：属于工具层面的单次编辑提示，不影响最终代码状态与运行结果。

## 03:01 MTC 文案中文化与验收指引完成
- 用户要求继续“下一项：MTC 诊断文案中文化 + 验收指引”。
- 已完成：
  - `index.html` 中 MTC 区域文案中文化：
    - 信号状态（发送中/空闲）
    - 诊断卡状态（输出中/已断流/错误）
  - `README_USER.md` 新增《MTC（MIDI 时间码）验收指引（外行版）》
    - 启用步骤
    - 观察点
    - 常见问题与处理
- 服务已重启生效。
- 提交记录（新工作区）：`688bd2f`
  - `docs/ui(mtc): localize MTC diagnostics to Chinese and add practical acceptance guide`

## 03:05 LTC/MTC 跳跃与 MTC 指示灯问题修复
- 用户反馈：
  1) LTC/MTC 时间码有毫秒级跳跃、不连贯
  2) MTC 正在输出但绿灯不亮、文字显示空闲
- 已完成修复（新工作区）：
  - LTC/MTC 增加时间追踪平滑（anchor + chasedSeconds）
    - 在状态包间隙用本地时钟连续推进，降低“跳点感”
  - MTC 指示灯判断改进：
    - 以 `pulseAtMs` 时间窗判断是否“发送中”，提升闪烁可靠性
  - Source active 判定放宽：active 字段缺失时默认在线，减少误判断流
- 已编译重启验证。
- 提交记录（新工作区）：`3a09764`
  - `fix(sync): smooth LTC/MTC chase timing and improve MTC pulse indicator reliability`

## 03:11 回归修复：时间码平滑策略与MTC灯判定
- 用户反馈：
  1) 时间码跳跃更明显、速度不均匀（比修改前更差）
  2) MTC状态输出中，但MTC灯仍显示空闲
- 已执行修复：
  - 回退过度平滑的 chase 逻辑（LTC/MTC）到更稳定时基
  - MTC 指示灯判定改为直接跟随 `outputState=OUTPUTTING` 并做前端节拍闪烁
- 已编译并重启服务。
- 提交记录（新工作区）：`1718fad`
  - `fix(regression): rollback over-aggressive chase smoothing and make MTC lamp follow output state`

## 03:21 异常处理机制方案评审（Phase-2.3 候选）
- 用户提出增强规则：
  - 全局 SyncHealthMonitor（设备/端口/网络丢包监控，UI红色告警）
  - 优雅停靠（停止/掉线时统一熄灭信号；暂停驻留）
  - 自愈重连（每5秒自动重连）
  - MTC FPS 锁频并记录抖动
- 已给出评审意见：
  1) 网络丢包阈值建议默认 250ms（可配置可切 100ms 严格模式）
  2) 停播行为建议配置化：`onStopTimecodeMode = hold | zero`
- 已建议按 Phase-2.3 立项落地的5个子模块：
  - SyncHealthMonitor
  - Health API + UI 状态栏
  - AutoReconnectManager
  - MTC Clock Lock + jitter log
  - Stop/Drop 行为策略器

## 03:24 停播行为最终规则确认
- 用户确认最终行为标准：
  1) 音乐暂停（PAUSE）：时间码停留最后一帧（驻留）
  2) 音乐停止/设备掉线/输出端口占用或故障/无效源：时间码归零
- 已确认将此作为后续实现的状态机规则，并同步文档。

## 03:25 执行确认与进度催促
- 用户确认“同意，执行”。
- 期间用户追问“好了没”。
- 已回复：尚未完成，进入直接落代码阶段；下一次回传将包含可验收结果（状态机生效场景 + 文档更新位置）。

## 03:30 状态机最终规则落地（Pause驻留 vs Stop归零）
- 用户催促立即执行后，已完成核心状态机落地：
  - PAUSE：驻留最后一帧（断流）
  - STOP/OFFLINE/无效源/端口故障：时间码归零、信号熄灭
- 实现方式：
  - `SyncOutputManager` 输出统一 `sourceState`（PLAYING/PAUSED/STOPPED/OFFLINE）
  - `LtcDriver` / `MtcDriver` 按 `sourceState` 执行驻留或归零策略
- 部署：已编译、重建、重启并确认状态字段返回。
- 提交记录（新工作区）：`b21e1fb`
  - `feat(sync-state): enforce pause-hold vs stop/offline-zero policy for LTC and MTC`

## 03:38 针对“Stop不归零/断开不归零/帧抖动”的修复
- 用户反馈当前问题：
  1) 按停止键后时间码未归零
  2) 手动关闭扫描（设备断开）后时间码未归零
  3) 时间码帧位抖动严重、不连贯
- 已完成修复（新工作区）：
  - `SyncOutputManager`：源状态判定增强
    - `playing=false 且 beat<0` 判定为 STOPPED
    - 否则可判定 PAUSED（驻留）
  - `LtcDriver` / `MtcDriver`：
    - source timeout/offline（`sourceFresh=false`）强制归零
    - 非 PAUSED 状态强制归零
    - 保留 PAUSED 驻留策略
  - 去除/减弱导致速度不均匀的追时逻辑，降低帧抖动
- 部署：已编译、重建、重启验证。
- 提交记录（新工作区）：`76b14ec`
  - `fix(sync): classify paused vs stopped via beat and force zero on source timeout/offline`

## 03:48 全局时间码组件与状态判定继续收敛
- 用户提出：
  1) LTC/MTC 共用同一个 Live Timecode 组件
  2) 帧抖动需回归稳健主从对齐（缓冲+线性平滑思路）
  3) 发送线程优先级提高
- 本轮已落地的关键部分：
  - `SyncOutputManager` 增加统一全局 timecode 输出（`getStatus().timecode`）
  - UI 改为优先显示全局 timecode（不再双轨数字分裂）
  - `sourceState` 状态链继续细化（PLAYING/PAUSED/STOPPED/OFFLINE）并贯穿驱动
  - Pause/Stop/Offline 行为继续收敛到最终策略
- 部署：已编译、重启通过。
- 提交记录（新工作区）：`0cd4094`
  - `feat(sync-ui): unify global live timecode and refine pause/stop/offline state handling`

## 03:57 切换到“纯透明传输模式”
- 用户要求全线逻辑精简，进入“所见即所得”模式：
  - 取消归零/状态强干预
  - 去掉补帧、平滑、预测
  - 收到 Pro DJ Link 时间即输出
  - UI 数字直接绑定后端输出变量
- 已执行：
  - `SyncOutputManager` 去除平滑器与预测链，仅保留原始时间透传
  - `LtcDriver` / `MtcDriver` 去除 displaySeconds 等补偿逻辑，直接使用原始 seconds
  - 保持 UI 使用后端统一 timecode
- 部署：已编译、重建、重启。
- 提交记录（新工作区）：`3367288`
  - `refactor(sync): switch to transparent pass-through mode without smoothing/prediction`

## 04:04 透明模式下帧跳跃现象解释
- 用户在 UI 观察到帧数跳跃并询问原因。
- 已说明：
  - 透明模式下不做平滑/补偿，UI 直接镜像原始时间包
  - 帧跳跃来自原始包到达间隔不均（设备发送节奏+网络抖动）
  - 该现象是“原始抖动可视化”的预期表现
- 已提出后续可选方案：在不影响真实 LTC/MTC 输出前提下，增加“仅 UI 显示平滑开关”。

## 04:06 演出系统漏触发风险确认
- 用户指出：MA2/TIAN 接收跳跃帧可能漏触发。
- 已明确认同并给出架构建议：
  - 输入层保持透明（用于观察原始抖动）
  - 输出层采用重定时稳定时钟（给控台稳定 LTC/MTC）
- 建议模式开关：`Output Mode = Transparent | Stable`
  - 演出默认 Stable
  - 调试使用 Transparent

## 04:09 双模式输出（Stable/Transparent）落地
- 用户同意执行双模式策略后，已实现并上线：
  - I/O Setup 新增 `outputMode` 下拉：
    - `Stable（演出）`
    - `Transparent（调试）`
  - 配置项：`sync.outputMode`（默认 stable）
  - `SyncOutputManager` 输出统一全局时间源：
    - `rawTimeSec`（原始）
    - `masterTimeSec`（当前模式输出时基）
    - `timecode`（UI与输出共用）
  - Stable 模式启用平滑时钟（5包窗口 + 30ms 缓冲）
  - Transparent 模式直接透传原始时间
- 部署验证：已编译、重建、重启。
- 提交记录（新工作区）：`6c92b10`
  - `feat(sync-mode): add stable/transparent output mode toggle with unified global timecode source`

## 04:15 用户要求回退到纯透明版本（已执行）
- 用户反馈双模式无效，要求回退到“纯透明传输”版本。
- 已执行：
  - 新工作区 `git reset --hard 3367288`
  - 重新编译、重建 classpath、重启 `djlink-webui.service`
- 当前运行版本确认：`HEAD=3367288`
  - `refactor(sync): switch to transparent pass-through mode without smoothing/prediction`

## 04:31 MTC 库来源确认
- 用户询问当前 MIDI 库使用情况。
- 已确认：MTC 使用 Java 内置 `javax.sound.midi`，未引入第三方 MIDI 依赖。
- 相关实现文件：`dbclient/src/main/java/dbclient/sync/drivers/MtcDriver.java`。

## 04:48 按7部分完成现状梳理与最小改动评估（未改代码）
- 用户要求：先停止改代码，仅做信息收集与现状梳理，按7部分模板输出。
- 已完成输出内容：
  1) 关键项目结构与职责（beat-link输入、状态聚合、MTC/LTC、scheduler、API/UI）
  2) 当前时间数据流（CDJ -> DeviceManager -> JettyScheduler -> SyncOutputManager -> MTC/LTC）
  3) 关键类清单与改动风险等级
  4) 关键代码片段（时间获取、MTC QF 发送、LTC 音频输出、定时循环）
  5) 修改影响评估（必须改/尽量不改/可新增）
  6) 最小改动方案（3改1增：`SyncOutputManager`+`MtcDriver`+`LtcDriver`，新增 `TimecodeClock`）
  7) 总结简表（可局部修复=是，风险点=source状态判定）
- 本轮按用户要求未执行代码修改动作。

## 04:58 按“3改1增”最小方案完成局部修复
- 用户正式批准按最小改动推进，并给出硬约束：
  - 不大改架构
  - 不动 DeviceManager 主流程
  - 不改 JettyServer 大框架
  - 不改现有 Web API/WebUI 数据结构
- 已按要求完成：
  - 新增：`dbclient/src/main/java/dbclient/sync/TimecodeClock.java`
  - 修改：
    1) `SyncOutputManager.java`（离散参考 -> ingestReference -> 连续时钟输出）
    2) `MtcDriver.java`（update 不直发；新增独立发送线程，固定 `1000/FPS/8` 节奏）
    3) `LtcDriver.java`（保留音频线程，仅替换时间源为 `clock.nowSeconds()`）
- 关键实现：
  - `TimecodeClock.ingestReference(...) / nowSeconds()`
  - 软/硬校正最小可用版
  - MTC 线程优先级与 LTC 线程优先级提升为 `Thread.MAX_PRIORITY`
  - MTC jitter 超阈值日志输出
- 编译/重启已完成。
- 提交记录（新工作区）：`c860787`
  - `fix(timecode): minimal 3+1 patch with unified TimecodeClock and independent MTC send loop`

## 05:07 3改1增实现细节与测试方案说明
- 用户要求：先不继续扩改，不切 RtMidi，仅补充实现细节与可操作测试方法。
- 已按6点详细说明：
  1) TimecodeClock 阈值/软硬校正/速度换算细节
  2) MtcDriver QF循环、发送策略、线程等待方式、FullFrame现状
  3) LtcDriver block时间基准、块时长、边界逻辑
  4) 当前日志能力与查看方式
  5) MTC/LTC 最小测试步骤及分层定位方法
  6) 下一步建议（先实测，再局部优化MTC/LTC，不先切RtMidi）
- 结论口径：当前应先做实测验证，再决定最小后续修补点。

## 05:11 最小双修：MTC锁帧 + LTC块连续承接
- 用户要求在最小改动原则下只做两项：
  1) MTC 一轮8个QF锁同一帧
  2) LTC block 边界改为连续承接并阈值重贴
- 已执行且仅修改两文件（未扩散）：
  - `dbclient/src/main/java/dbclient/sync/drivers/MtcDriver.java`
  - `dbclient/src/main/java/dbclient/sync/drivers/LtcDriver.java`
- MTC变更：
  - `qfCounter==0` 时锁定 `hh/mm/ss/ff`
  - `qfCounter 1..7` 使用同一锁定帧发送，避免同轮混帧
  - 新增字段：`lockedHh/lockedMm/lockedSs/lockedFf`
- LTC变更：
  - 新增 `nextBlockStartSec` 连续承接
  - 后续块优先 `+ blockDurationSec`
  - 偏差超阈值才重贴（阈值 20ms）
  - 新增字段：`blockClockInit/nextBlockStartSec/BLOCK_RELOCK_THRESHOLD_SEC`
- 部署：编译、重建、重启成功。
- 提交记录（新工作区）：`7392e2c`
  - `fix(timecode): lock MTC 8-QF frame group and add LTC block carry-forward with threshold relock`

## 14:23 网页打不开问题修复
- 用户反馈 Web 页面打不开。
- 排查结果：
  - 服务进程在跑，但 `dbclient` 目录缺少编译产物与 `.runtime.cp`，导致静态页面返回 404。
- 已执行恢复：
  1) `mvn -q compile -DskipTests`
  2) `scripts/build-dbclient-runtime.sh`
  3) `systemctl --user restart djlink-webui.service`
- 验证结果：
  - `/` 返回 200
  - `/index.html` 返回 200
- 用户可通过刷新页面恢复访问。

## 14:24 LAN 访问 404 排查结果
- 用户反馈在 `http://192.168.100.200:8080/` 看到 404。
- 本机实测结果：
  - `hostname -I` 包含 `192.168.100.200`
  - 8080 端口由 Java 进程监听
  - `curl -i http://192.168.100.200:8080/` 返回 `HTTP/1.1 200 OK`（Jetty）
- 已判断：问题更可能出在访问端（缓存/代理/命中错误主机）而非服务端不可用。
- 已给用户排查建议：访问端执行 `curl -i` 验证并回传响应头 `Server` 字段。

## 14:34 MTC 问题收窄与日志增强（仅 MtcDriver）
- 用户要求先收窄问题，不做大改：
  - 检查 QF 编码顺序与锁帧逻辑
  - 增强可开关 debug 日志
  - 评估发送层抖动风险
  - 给出原因概率排序与最小下一刀
- 已执行（仅改 `MtcDriver`）：
  - 新增 `debug` 开关（配置 `sync.mtc.debug=true`）
  - 日志输出每个QF细节：
    - qfCounter、锁定时码 hh:mm:ss:ff、data byte、发送时间、jitter、lateCycles
  - 发送循环增加 `lateCycles` 跳周期处理，避免追赶时挤包
- 已确认编码顺序符合 MTC 规范，且实现“8个QF锁同一帧”。
- 已给出原因排序：发送线程调度抖动 > 接收端刷新策略 > fps不一致 > 编码错误。

## 14:42 /api/sync/state timecode 来源与采样关系澄清
- 用户要求先不扩改，只澄清 timecode 跳跃是否来自显示采样而非真实输出。
- 已核查并说明：
  1) `/api/sync/state.timecode` 由 `SyncOutputManager` 生成（`toTimecode(clock.nowSeconds(),25)`）
  2) `rawTimeSec` 与 `timecode` 非同一采样时刻（前者为最近参考，后者为请求时 clock 实时值）
  3) WS 与前端 sync 刷新均为 100ms；在 25fps 下天然可能每次跳 2~3 帧
  4) 对 `rawTimeSec=32.393` 计算：`0.393*25=9.825`，取整约 09 帧；若页面见 11 帧，多因采样时刻不同
  5) 明确区分 UI采样频率 vs MTC发送频率（5ms级）
  6) 给出最小方案：在 MTC status 增加调试字段（qfCounter/locked帧/data/jitter/lateCycles）以证据化判断

## 14:49 MTC 发送层可观测性增强（仅 MtcDriver）
- 用户要求仅做最小调试增强，不改发送逻辑与整体架构。
- 已仅修改 `MtcDriver`，在 `/api/sync/state -> drivers.mtc` 增加诊断字段：
  - `qfCounter`
  - `lockedHh/lockedMm/lockedSs/lockedFf`
  - `lastDataByte`
  - `lastSendTsMs`
  - `lastJitterMs`
  - `lateCycles`
  - `roundId`
  - `recentMaxJitterMs`
  - `recentAvgJitterMs`
- 同时保留可开关 debug 日志能力（`sync.mtc.debug=true`）。
- 编译、重建、重启与字段回读验证完成。
- 提交记录（新工作区）：`a99c343`
  - `chore(mtc-observability): expose qf lock/jitter/round diagnostics in status`

## 19:40 Ableton Link 最小可用方案评估（未开改）
- 用户要求进入 Phase-2.2 Ableton Link，实现最小可用且不扩散改动。
- 已完成现状评估：
  - 当前 `AbletonLinkDriver` 为占位实现（仅 tempo 字段）
  - 项目未接入任何 Ableton Link 库
  - 可复用现有 `SyncOutputManager` 字段：`masterBpm/masterTimeSec/sourcePlaying/sourcePlayer/sourceState`
  - 现有 `TimecodeClock` 可作为 Link 时间参考
- 已给出建议接法（先方案，不开改）：
  - Java 主程序 + 独立 Ableton Link Bridge 进程（IPC）
  - 原因：Java 无官方稳定 Link 原生实现，桥接方式风险更低且最小侵入
- 建议最小改动范围：
  - 修改 2 文件：`AbletonLinkDriver.java`、`UserSettingsStore.java`
  - 新增 1 文件：`AbletonLinkBridgeClient.java`
- 第一阶段目标：tempo/beatPosition/playing 基础同步 + status 输出（running/enabled/tempo/beatPosition/playing/numPeers/lastUpdateTs/error）。

## 19:44 Ableton Link 第一阶段设计稿
- 用户认可“Java主程序 + 独立Link Bridge”路线，并要求先提交设计稿再编码。
- 已提交设计要点：
  1) Bridge 方案：Node.js + abletonlink（快速落地，后续可替换C++ bridge）
  2) 通信：localhost UDP + JSON（Java->bridge sync；bridge->Java ack）
  3) 第一阶段数据流：
     - Java 从 `state` 取 `masterBpm/masterTimeSec/sourcePlaying/sourcePlayer/sourceState`
     - `beatPosition = masterTimeSec * tempo / 60`
     - bridge 回报 `running/numPeers/lastAckTs/error`
  4) 失败保护：bridge 不在线不影响主服务；5秒重连
  5) 调试方案：通过 `/api/sync/state -> drivers.abletonLink` 验证 peers/tempo/beatPosition
- 保持最小改动承诺：仅改 `AbletonLinkDriver`、`UserSettingsStore`，新增 `AbletonLinkBridgeClient`。

## 19:49 Ableton Link 设计稿重发（精简版）
- 用户反馈“未收到设计稿”，已重发精简版并等待确认。
- 重发内容包含：
  - 选型：Java + Node Link Bridge
  - 通信：localhost UDP + JSON
  - 数据流：masterBpm/masterTimeSec -> beatPosition -> bridge ack
  - 失败保护：bridge 不在线不影响 LTC/MTC，5秒重连
  - 最小改动范围：2改1增（AbletonLinkDriver、UserSettingsStore、AbletonLinkBridgeClient）

## 19:56 Ableton Link 第一阶段 Java 侧最小实现完成
- 用户确认按最小路线开工并追加4点约束（固定公式、简化playing、固定UDP JSON字段、最小调试能力）。
- 已完成实现（仅2改1增）：
  - 修改：
    1) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkDriver.java`
    2) `dbclient/src/main/java/dbclient/config/UserSettingsStore.java`
  - 新增：
    3) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeClient.java`
- 已落实协议：
  - Java->bridge：`tempo/beatPosition/playing/timestamp/sourcePlayer/sourceState`
  - bridge->Java：`running/numPeers/lastAckTs/error`
- 已落实调试可观测字段（`/api/sync/state -> drivers.abletonLink`）：
  - `running/enabled/tempo/beatPosition/playing/sourcePlayer/sourceState/lastUpdateTs/numPeers/lastAckTs/error`
- 失败保护：bridge 不在线不影响主服务，driver 内 5 秒重连。
- 部署验证：编译、重建、重启成功；当前默认 `enabled=false`，状态可读。
- 提交记录（新工作区）：`ae1dfe7`
  - `feat(link-phase1): minimal UDP JSON bridge client and ableton link driver status pipeline`

## 20:02 Ableton Link Bridge 第一版脚本交付（最小）
- 用户要求“只补最小 bridge 进程脚本，不扩展别的功能”。
- 已新增脚本：
  - `scripts/ableton-link-bridge.js`
- 功能范围（第一版）：
  - 监听 Java 侧 UDP JSON（默认 `127.0.0.1:19110`）
  - 解析字段：`tempo/beatPosition/playing/timestamp/sourcePlayer/sourceState`
  - 回 ACK 到 Java（默认 `127.0.0.1:19111`），字段：`running/numPeers/lastAckTs/error`
  - 提供最小日志：接收值、rxTs、ackTs、numPeers、error
- 兼容模式：
  - 未安装 `abletonlink` 时运行 ACK-only（可先验证通信链路）
  - 安装后尝试启用真实 Link backend（best-effort peers 读取）
- 本地自测：
  - 发送测试 UDP 包，成功收到 ACK：`{"type":"ack","running":true,"numPeers":0,"lastAckTs":...,"error":""}`
- 提交记录（新工作区）：`05f9a68`
  - `feat(link-bridge): add minimal local UDP JSON bridge script with ack logging`

## 20:11 Bridge SIGTERM 日志解释
- 用户看到系统消息 `Exec failed ... SIGTERM` 并质疑状态。
- 已澄清：这是测试后主动终止 bridge 进程导致的工具状态，不代表功能失败。
- 根据日志确认链路成功：
  - bridge 已监听 `127.0.0.1:19110`
  - 已收到 Java/测试 UDP 包（rx tempo/beat/playing）
  - 已成功回 ACK
- 结论：当前已验证 `Java -> bridge -> ACK` 通信链路打通；尚处 ACK-only（未安装 abletonlink 依赖）。

## 20:16 Ableton Link 配置与最小测试步骤说明（无代码改动）
- 用户要求仅补配置方法和测试步骤，不扩展功能。
- 已提供：
  1) 配置文件位置：`config/user_settings.json` 下 `sync.abletonLink`
  2) `enabled=false -> true` 修改方式
  3) host/port 键名：`bridgeHost/bridgeSendPort/bridgeListenPort`
  4) 建议改完后重启：`systemctl --user restart djlink-webui.service`
  5) 两段最小测试流程：
     - 先验证 Java->bridge->ACK（看 `lastAckTs/error/tempo/beatPosition`）
     - 再验证真实 Link peers（安装 `abletonlink`、启用 Link 客户端、看 `numPeers`）

## 20:24 Ableton Link WebUI 最小图形化配置完成
- 用户要求不再手改 JSON/命令行，改为 WebUI I/O Setup 直接配置与测试。
- 已仅改前端文件：
  - `dbclient/src/main/resources/web/index.html`
- 新增 UI 控件（I/O Setup / Sync Output Manager 区）：
  - `Ableton Link Enabled`
  - `Bridge Host`
  - `Bridge Send Port`
  - `Bridge Listen Port`
  - `linkDiag` 状态显示区
- 已接入现有配置与热重载链路：
  - `loadSettings()` 读取 `sync.abletonLink` 的 host/port/enabled
  - `saveIoConfig()` 保存 `abletonLink.enabled/bridgeHost/bridgeSendPort/bridgeListenPort`
  - 保存提示更新为“Sync 输出已热重载”
  - `refreshSyncState()` 渲染 Link 状态（running/enabled/tempo/beatPosition/playing/numPeers/lastAckTs/error）
- 后端主结构未扩改（符合约束）：
  - 未动 DeviceManager / JettyServer / SyncOutputManager 主结构
  - 未动 MTC/LTC 逻辑
- 已编译、重建、重启并验证页面包含新控件。
- 提交记录：`48aae8c`
  - `feat(web-io): add ableton link bridge host/port controls and live status panel`

## 20:30 Bridge 启动方式确认（无代码改动）
- 用户确认 WebUI 版本可用，暂不扩展功能，仅询问 bridge 是否需手动启动。
- 已回复：当前 bridge 仍需单独手动启动。
- 最简启动命令：
  - `cd /home/shenlei/.openclaw/agents/dev/workspaces/dj-link-stage`
  - `node scripts/ableton-link-bridge.js`
- 已说明最先观察字段：
  - `drivers.abletonLink.lastAckTs`（及 linkDiag 的 ack 时间）
  - 该字段持续更新即表明 Java->bridge->ACK 通路打通。

## 20:32 WebUI 直控 bridge 需求确认（设计答复）
- 用户要求：不手动命令行，全部在 WebUI 完成 Ableton Link bridge 启停与测试。
- 已确认可行，并给出最小改动设计：
  - 前端：仅改 `dbclient/src/main/resources/web/index.html`
    - 增加 Start Bridge / Stop Bridge 按钮
    - 显示 Bridge Running 状态（可并入 linkDiag）
  - 后端最小必要：
    - `AbletonLinkDriver.java` 增加 bridge 进程控制入口
    - `JettyServer.java` 增加轻量 API（start/stop/status）
    - 可选新增 `AbletonLinkBridgeProcess.java` 封装 ProcessBuilder
- 安全与稳定承诺：
  - bridge 为独立子进程，失败仅反映 error，不影响主服务
  - start/stop 幂等处理
  - 不改 DeviceManager、不动 MTC/LTC、不重构主链路
- 结论：可做到用户完全不用手动执行命令或编辑配置文件。

## 20:34 WebUI 直控 Ableton Link Bridge 实现完成
- 用户要求：完全通过 WebUI 启停/测试 bridge，不使用终端命令。
- 已实现（最小改动）：
  - 前端：`dbclient/src/main/resources/web/index.html`
    - 新增按钮：`Start Bridge` / `Stop Bridge`
    - 新增 Bridge 状态卡：显示 running/pid/started/error
    - 接入按钮事件调用后端 API
  - 后端：
    - 新增 `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeProcess.java`
      - `ProcessBuilder` 管理 `scripts/ableton-link-bridge.js`
      - 输出到 `../logs/ableton-link-bridge.log`
      - 提供状态：`bridgeRunning/bridgePid/bridgeStartedAt/bridgeError`
    - 修改 `AbletonLinkDriver.java`：暴露 bridge start/stop/status，状态合并到 driver.status()
    - 修改 `SyncOutputManager.java`：新增 abletonLink bridge 控制转发方法
    - 修改 `JettyServer.java`：新增 API
      - `POST /api/sync/ableton-link/bridge/start`
      - `POST /api/sync/ableton-link/bridge/stop`
      - `GET /api/sync/ableton-link/bridge`
- 已满足字段要求：
  - `/api/sync/state -> drivers.abletonLink` 包含
    - `bridgeRunning`
    - `bridgePid`
    - `bridgeStartedAt`
    - `bridgeError`
- 幂等性验证通过：
  - start 两次：第二次返回 `already running`
  - stop 两次：第二次返回 `already stopped`
- 主服务保护：bridge 异常仅写 `error/bridgeError`，不影响其他 driver。
- 编译/重建/重启及 API 实测通过。
- 提交记录：`d2652f0`
  - `feat(link-web-control): add web start/stop bridge controls and bridge process status`

## 20:43 Ableton Link 当前状态判读与下一步验证指引
- 用户反馈状态：enabled=true、bridgeRunning=true、bridgePid有值、lastAckTs有值、error空、tempo/beatPosition正常、numPeers=0。
- 已确认结论：WebUI -> Java -> bridge -> ACK 链路已打通。
- 已答复（不改代码）：
  1) 当前更可能仍是 ACK-only/弱接入，尚不能仅凭 ACK 证明真实 Link session 已加入。
  2) 真实 Link backend 需要条件：`abletonlink` 依赖可加载并成功初始化、同网段有启用 Link 的对端、网络/防火墙允许发现流量。
  3) 验证 numPeers>0 的方法：保持 bridge 运行，打开 Live/Link 客户端，观察 `/api/sync/state` 与 bridge 日志 peers 变化。
  4) 若仍为 0，最可能原因：
     - 真实 Link backend 未成功启用
     - 对端软件 Link 未实际打开
     - 组播/防火墙受限
     - bridge 与库版本导致 peers API 读取不匹配

## 20:47 Ableton Link backend 真伪确认 + peers 可观测性增强
- 用户要求本轮仅做 backend 真伪确认与 peers 观测增强，不扩展其他功能。
- 已做最小改动：
  - `scripts/ableton-link-bridge.js`
  - `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeClient.java`
  - `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkDriver.java`
- 新增状态字段（`/api/sync/state -> drivers.abletonLink`）：
  - `backendMode` (`ack-only|abletonlink-active|abletonlink-failed`)
  - `backendLoaded` (bool)
  - `backendVersion` (string)
  - `peerDetectionWorking` (`true|false|unknown`)
  - `backendInitError` (string)
- 实测结论（当前环境）：
  - `backendMode=ack-only`
  - `backendLoaded=false`
  - 日志明确：`abletonlink package not installed; running ACK-only mode`
  - 因此 `numPeers=0` 主要由 backend 未启用导致，不是仅“无peer”。
- 已提交：`a6c1f8d`
  - `chore(link-observability): expose backend mode/load/init diagnostics and peer detection status`

## 20:52 Ableton Link backend 从 ack-only 切到真实 active（已完成）
- 用户要求停止分析，直接解决：安装依赖并让 bridge 进入真实 Ableton Link backend。
- 已完成：
  1) 在工作区根目录创建 Node 小项目：`package.json` / `package-lock.json`
  2) 安装依赖：`abletonlink`（npm install 成功）
  3) 更新 `scripts/ableton-link-bridge.js` 启动逻辑：
     - 优先加载真实 abletonlink backend
     - 仅在失败时 fallback 到 ack-only
     - 明确日志：`abletonlink loaded` / `backend init success` / `fallback to ack-only: reason`
     - 错误原文写入 `backendInitError` 与 `error` 路径
- 复测结果（Web/API）：
  - `backendMode=abletonlink-active`
  - `backendLoaded=true`
  - `backendVersion=0.2.0-beta.0`
  - `peerDetectionWorking=true`
  - `backendInitError=""`
  - `bridgeError=""`
- 结论：此前 Resolume 看不到 Link 的根因是 ack-only（包未安装）；当前已进入真实 backend active。
- 提交记录：`19e3cde`
  - `build(link-backend): install abletonlink runtime deps and enforce explicit backend mode logs/errors`

## 21:56 Bridge 存活与状态一致性修复
- 用户反馈实际状态回退：`bridgeRunning=false`, `backendMode=ack-only`，要求停止网络分析，直接修 bridge 存活与可观测。
- 已完成最小修复（不动 DeviceManager/MTC/LTC/主结构）：
  - 修改 `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeProcess.java`
    - 增加进程退出/诊断字段：
      - `bridgeExitCode`
      - `bridgeLastExitReason`
      - `bridgeLastStdout`（截断）
      - `bridgeLastStderr`（截断）
    - 启动后短等待检测“瞬间退出”，避免假成功
    - 分离抓取 stdout/stderr，解析 backend hint（active/fallback/init failed）
  - 修改 `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkDriver.java`
    - 将上述字段透传到 `/api/sync/state -> drivers.abletonLink`
    - 修正状态覆盖逻辑：仅在收到 ACK 后才用 bridge client 的 backend 字段覆盖，避免“真实 active 被默认 ack-only 覆盖”
- 实测结果：
  - Start 后可见 `bridgeRunning=true`, `bridgePid>0`
  - 启动即读到 `backendMode=abletonlink-active`, `backendLoaded=true`, `backendInitError=''`
  - Stop 后可见退出码与退出原因（例 `code=143`）
- 提交记录：`d8f383e`
  - `fix(link-bridge): keep real backend status consistent and expose process exit/stdout/stderr diagnostics`

## 22:08 code 143 语义修复：区分正常 Stop 与异常退出
- 用户反馈 WebUI 显示 `Bridge: stopped` 与 `bridge exited with code 143`，要求先修 bridge 生命周期语义，不再误导。
- 已确认：143 为 SIGTERM，通常由 API Stop 主动触发。
- 已做最小修复（仅 bridge 进程管理）：
  - 修改 `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeProcess.java`
  - 新增 `expectedStop` 标记，区分手动 stop 与异常退出
  - 若为 API Stop 导致的 143/0：
    - `bridgeLastExitReason` 置为 `stopped by API`
    - `bridgeError` 清空（不再当异常报错）
- 回归验证：
  - Start 后：`bridgeRunning=true`, `bridgePid>0`, `backendMode=abletonlink-active`, `backendLoaded=true`
  - Stop 后：`bridgeExitCode=143`, `bridgeLastExitReason='stopped by API'`, `bridgeError=''`
- 提交记录：`63d4340`
  - `fix(link-bridge-lifecycle): treat SIGTERM 143 as normal API stop and keep exit reason explicit`

## 22:23 Peer 问题收敛结论与下一步建议
- 用户确认当前链路已通：Java -> bridge -> 真实 Ableton Link backend（active）。
- 当前唯一剩余问题：`numPeers=0`，问题已收敛到 peer 发现层。
- 已给出判断：
  - 更可能是当前 Node `abletonlink` beta 包的 peers 可见性/兼容性问题
  - “确实无可发现 peer”次之
- 已给出最小下一步建议：
  - 不再深挖现有 beta 包 peers API
  - 采用“最小替代”路线：保留 Java/WebUI/UDP JSON 协议不变，仅替换 bridge 内核为更可靠实现（优先 C++ Ableton Link SDK）

## 22:26 C++ 官方 Link bridge 替换方案（设计稿）
- 用户决定不再深挖 Node beta peers，改为“只替换 bridge 内核，不换系统接口”。
- 已提交最小设计稿：
  - 新增 C++ 目录：`bridge-cpp/`
  - 最小文件：`CMakeLists.txt`、`src/main.cpp`、`src/UdpJsonBridge.{h,cpp}`（可选 `LinkEngine`）
  - 可执行产物：`bin/ableton-link-bridge`
  - 编译方式：CMake Release 构建并复制到 `bin/`
  - 对接方式：保持 WebUI Start/Stop API 不变，仅把 Java 进程管理启动命令从 node 脚本切换到 C++ 可执行文件
  - Java 最小改动：
    1) `AbletonLinkBridgeProcess.java`（启动命令）
    2) `AbletonLinkDriver.java`（仅可选兼容字段）
- 说明了可靠性优势：官方 C++ 实现更接近 Link 协议核心，避免 Node beta peers 可见性/兼容性问题，同时保留现有 Java/WebUI/UDP JSON 接口不变。

## 22:33 切换到 C++ 官方 Link bridge（第一版已跑通）
- 用户确认按“只替换 bridge 内核，不换系统接口”开工，并强调第一版先保证进程/ACK/numPeers回传。
- 已完成：
  - 新增 C++ bridge：
    - `bridge-cpp/src/main.cpp`
    - `bridge-cpp/CMakeLists.txt`
    - `scripts/build-bridge-cpp.sh`
  - Java 进程管理切换：
    - `AbletonLinkBridgeProcess` 启动目标改为 `bin/ableton-link-bridge`
  - 保持 UDP JSON 协议兼容：
    - Java->bridge：`tempo/beatPosition/playing/timestamp/sourcePlayer/sourceState`
    - bridge->Java ACK：`running/numPeers/lastAckTs/error/backendMode/backendLoaded/backendInitError`（含 backendVersion）
- 构建细节：
  - 运行环境无 cmake，已在脚本中加入 g++ 直编 fallback
  - 产物：`bin/ableton-link-bridge`
- 实测：
  - Start 后 `bridgeRunning=true`、`bridgePid>0`
  - `backendMode=abletonlink-active`、`backendLoaded=true`
  - `backendInitError=''`
  - 状态字段链路兼容保持
- 同时修复了 stop 可能超时问题（进程等待增加超时 + 强杀兜底），Stop 后 `bridgeLastExitReason='stopped by API'`，不误报 bridgeError。
- 提交记录：`eef8efb`
  - `feat(link-cpp-bridge): switch bridge process to native binary with official Link SDK integration`

## 22:53 修复 C++ bridge 被 SIGKILL(137) 误杀
- 用户反馈：bridge 启动后被 kill（exit 137），要求只修进程生命周期管理。
- 根因定位：
  - Stop/重载时管理器先发 SIGTERM；C++ bridge 阻塞在 `recvfrom`，未及时退出；
  - 触发 `destroyForcibly()`，导致 exit 137（管理器强杀），并非 Link backend 本体问题。
- 修复内容（最小范围）：
  1) `bridge-cpp/src/main.cpp`
     - 给 UDP socket 增加 `SO_RCVTIMEO=200ms`
     - 循环处理 `EAGAIN/EWOULDBLOCK/EINTR`，收到终止信号后可快速退出
  2) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeProcess.java`
     - stop 等待窗口增大（先优雅退出，再必要时强杀）
     - 增加 `bridgeKilledByManager` 字段
     - 增加进程序号保护，避免旧进程状态污染新进程
  3) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkDriver.java`
     - 透传 `bridgeKilledByManager` 到 `/api/sync/state`
- 回归结果：
  - Start 后 1s/3s/6s 持续 `bridgeRunning=true`、`bridgePid` 稳定、`backendMode=abletonlink-active`
  - Stop 后正常 `bridgeExitCode=0`、`bridgeLastExitReason='stopped by API'`、`bridgeKilledByManager=false`
- 提交记录：`1f2a874`
  - `fix(link-bridge-stability): prevent forced-kill by graceful socket timeout shutdown and add manager-kill visibility`

## 23:16 Peer 发现层后续计划（仅范围确认）
- 用户确认当前桥接链路整体健康：
  - `bridgeRunning=true`, `backendMode=abletonlink-active`, `backendLoaded=true`, `backendVersion=ableton-link-cpp`
  - `bridgeKilledByManager=false`, `bridgeExitCode=0`
  - `lastAckTs` 正常更新，`error/bridgeError` 为空
- 当前唯一问题：`numPeers=0`。
- 已按用户要求仅给 peer 层方案，不改其它：
  1) 可能原因排序：
     - 真无可发现 peer（最高）
     - 对端未真正加入 Link
     - C++ bridge peers 读取时机/采样问题
     - discovery 层限制（组播/接口）
  2) 改动范围仅 C++ bridge 文件（`bridge-cpp/src/main.cpp` 为主）
  3) 先做更细 peer 观测，再决定是否改逻辑
  4) 判定信号建议：`maxPeersSeen/lastPeerChangeTs/peerSampleCount`，区分“真无peer”与“读取逻辑问题”

## 23:19 Peer 观测增强（仅 peer 层）
- 用户要求继续只收敛在 peer 发现层，先做观测增强不盲改大逻辑。
- 已完成改动（仅3文件）：
  1) `bridge-cpp/src/main.cpp`
  2) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeClient.java`
  3) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkDriver.java`
- 新增 peer 观测字段（ACK -> `/api/sync/state -> drivers.abletonLink`）：
  - `maxPeersSeen`
  - `lastPeerChangeTs`
  - `peerSampleCount`
  - （保留）`numPeers`
- 字段语义：
  - `maxPeersSeen`：启动以来最大 peer 值
  - `lastPeerChangeTs`：peer 计数最后变化时间
  - `peerSampleCount`：累计采样次数
- 已编译部署并验证字段存在。
- 提交记录：`36595aa`
  - `feat(link-peer-observability): add maxPeersSeen/lastPeerChangeTs/peerSampleCount via cpp ack pipeline`

## 23:28 发现层问题结论与最小排障方向
- 用户给出关键观测：`numPeers=0`, `maxPeersSeen=0`, `lastPeerChangeTs=0`, `peerSampleCount=754`。
- 已确认结论：bridge 稳定采样但从未发现任何 peer，问题收敛到 discovery 层而非采样抖动。
- 已按用户要求（不改主链路）给出最小排障建议：
  1) 可能性排序：
     - 本机/网段组播发现不互通（最高）
     - Resolume 未真正进入可发现 Link 状态
     - 多网卡下 discovery 走错接口
     - 防火墙/系统策略拦截 UDP 组播
  2) 本机最小修法：同网卡同子网、放通组播 UDP、避免 VPN/虚拟网卡抢路由、确保 bridge 单实例
  3) Resolume 最小处理：确认 Link 真正 enabled 且引擎在运行态，必要时用另一 Link 客户端做 A/B
  4) 下一步建议：在 C++ bridge 增加最小网络接口/发现日志（网卡枚举 + peer callback）以确认 discovery 实际走向。

## 23:32 Discovery 可观测性增强（仅 C++ bridge 相关）
- 用户要求仅增强网络接口/discovery 可观测性，不动主链路与UI结构。
- 已改 3 文件：
  1) `bridge-cpp/src/main.cpp`
  2) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkBridgeClient.java`
  3) `dbclient/src/main/java/dbclient/sync/drivers/AbletonLinkDriver.java`
- 新增回传字段（ACK -> `/api/sync/state -> drivers.abletonLink`）：
  - `firstPeerSeenTs`
  - `lastPeerEventTs`
  - `peerEventCount`
  - `interfacesSummary`
  - `primaryInterfaceHint`
  - `discoveryActive`
- C++ bridge 侧行为：
  - 启动时枚举并日志输出网卡（name+IPv4）
  - 监听 peers 变化并记录事件计数与时间戳
- 已编译部署并提交：`8b2c207`
  - `feat(link-discovery-observability): add interface summary and peer event timestamps/counters to ack status`

## 23:45 Discovery 事件为 0 的收敛判断与最小下一步
- 用户给出关键状态：`discoveryActive=true` 但 `numPeers/maxPeersSeen/peerEventCount` 全为0，且 `peerSampleCount` 持续增长。
- 已确认：问题不是采样抖动，是 discovery 层无 peer 事件进入。
- 用户要求不改 Java/WebUI/生命周期，仅继续 discovery 层。
- 已给出最小方案（仅改 `bridge-cpp/src/main.cpp`）：
  1) 接口收敛：优先物理网卡（enp2s0）观测视角，弱化 docker/br 干扰
  2) 增强原始日志：周期输出 peers/event/interfaces/primary hint，明确是否出现任何 discovery 事件
- 原因排序建议：先查多网卡/docker 干扰，再看 Resolume 是否真实广播 Link。
